# @parsify/converter-xlsx

XLSX converter for [Parsify](https://github.com/parsify/parsify), built on
`exceljs`. Each worksheet becomes a heading plus a table block. Lazy-loaded.

Requires `exceljs` as a peer dependency. MIT
